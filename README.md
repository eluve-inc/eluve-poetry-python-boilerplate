# Getting Started

## Install Prerequisites (macOS)

```
# Install pipx
brew install pipx

# Install poetry
pipx install poetry

# Install packages
poetry install
```

## Configure Environment Variables

```
cp .env.example .env
```